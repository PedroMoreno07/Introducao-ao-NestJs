services:
  postgres:
    image: postgres
    container_name: postgresdb
    environment:
      POSTGRES_USER: pedrodb
      POSTGRES_PASSWORD: drope007
      POSTGRES_DB: nestdb
    ports:
      - '5432:5432'
